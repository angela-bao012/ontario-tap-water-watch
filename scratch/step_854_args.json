{
  "Description": "\"Restore totalTests and exceedances counts from PostgreSQL for search results and distinct location lists, ensuring status badges display correctly.\"",
  "Instruction": "\"Update distinctLocations and search location API endpoints to query and return real test counts and exceedance counts from the database.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":371,\"ReplacementContent\":\"  app.get(\\\"/api/locations/search\\\", async (req, res) => {\\n    try {\\n      const q = (req.query.q as string || \\\"\\\").trim().toLowerCase();\\n      if (q.length < 2) return res.json([]);\\n\\n      const filtered = cachedLocations.filter(loc => \\n        loc.location.toLowerCase().includes(q) ||\\n        (loc.display_name && loc.display_name.toLowerCase().includes(q))\\n      );\\n\\n      const paginated = filtered.slice(0, 40);\\n      const locationNames = paginated.map(p => p.location);\\n\\n      let countsMap = new Map<string, { total_tests: number, exceedance_count: number }>();\\n      if (locationNames.length > 0) {\\n        const countsRes = await pool.query(`\\n          SELECT location, \\n                 COUNT(*)::integer as total_tests, \\n                 SUM(CASE WHEN exceedance = 'Y' THEN 1 ELSE 0 END)::integer as exceedance_count\\n          FROM water_quality_data\\n          WHERE location = ANY($1)\\n          GROUP BY location\\n        `, [locationNames]);\\n        for (const row of countsRes.rows) {\\n          countsMap.set(row.location, {\\n            total_tests: row.total_tests,\\n            exceedance_count: row.exceedance_count\\n          });\\n        }\\n      }\\n\\n      const enriched = paginated.map((row) => {\\n        const loc = row.location;\\n        const disp = row.display_name;\\n        const dws_id = hashCode(loc);\\n        dwsIdToLocation.set(dws_id, loc);\\n\\n        const counts = countsMap.get(loc) || { total_tests: 0, exceedance_count: 0 };\\n\\n        return {\\n          dws_id,\\n          dws_name: loc,\\n          display_name: disp,\\n          owner_name: row.owner_name,\\n          sample_types: [],\\n          phu_name: \\\"Ontario Water System\\\",\\n          dws_category: \\\"Municipal Residential\\\",\\n          regulation: \\\"O. Reg. 170/03\\\",\\n          city_hint: null,\\n          totalTests: counts.total_tests,\\n          exceedances: counts.exceedance_count,\\n          passed: counts.total_tests - counts.exceedance_count,\\n    \n<truncated 4847 bytes>",
  "TargetFile": "\"/Users/angelabao/ontario-tap-water-watch/server.ts\"",
  "toolAction": "\"Updating search endpoints in server.ts\"",
  "toolSummary": "\"Edit server.ts file\""
}